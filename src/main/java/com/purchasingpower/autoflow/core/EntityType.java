package com.purchasingpower.autoflow.core;

/**
 * Entity types in the knowledge graph.
 *
 * @since 2.0.0
 */
public enum EntityType {
    CLASS,
    INTERFACE,
    ENUM,
    RECORD,
    METHOD,
    CONSTRUCTOR,
    FIELD,
    FILE,
    ENDPOINT,
    TEST
}
